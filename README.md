# data-processing-with-dropdowns
It reads CSV files, splits them into smaller chunks, and writes the data to Excel files. Each Excel file is enriched with dropdown validation for category selection. 
